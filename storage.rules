rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /issue-report-evidence/{allPaths=**} {
      allow read, write: if false;
    }

    match /tasks/{taskId}/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if false;
    }
  }
}
