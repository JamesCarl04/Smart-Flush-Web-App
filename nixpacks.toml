[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd mqtt-listener && npm install"]

[phases.build]
cmds = ["echo 'No build step - ts-node compiles at runtime'"]

[start]
cmd = "cd mqtt-listener && npx ts-node src/index.ts"
